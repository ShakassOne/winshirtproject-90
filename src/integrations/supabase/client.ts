
// This file is automatically generated. Do not edit it directly.
import { createClient } from '@supabase/supabase-js';
import type { Database } from './types';

const SUPABASE_URL = "https://aquxtqmotbiimahboqlo.supabase.co";
const SUPABASE_PUBLISHABLE_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFxdXh0cW1vdGJpaW1haGJvcWxvIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NDY0NTA1MzcsImV4cCI6MjA2MjAyNjUzN30.znxP9tr78wQPVVFVMj2BLYaVOGHRzde9X0MUnRzeoxY";

// Import the supabase client like this:
// import { supabase } from "@/integrations/supabase/client";

export const supabase = createClient<Database>(SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY);

// Required tables for the application
export const requiredTables = ['lotteries', 'lottery_participants', 'lottery_winners', 'products'];

// Valid table names type
export type ValidTableName = 
  | 'lotteries' 
  | 'lottery_participants' 
  | 'lottery_winners' 
  | 'products' 
  | 'visuals'
  | 'visual_categories'
  | 'orders'
  | 'order_items'
  | 'clients';

// Check Supabase connection function
export const checkSupabaseConnection = async (): Promise<boolean> => {
  try {
    const { data, error } = await supabase.from('lotteries').select('count').limit(1);
    return !error;
  } catch (error) {
    console.error("Error checking Supabase connection:", error);
    return false;
  }
};

// Check if required tables exist
export const checkRequiredTables = async (): Promise<{ exists: boolean, missing: string[] }> => {
  try {
    const missing: string[] = [];
    
    for (const table of requiredTables) {
      try {
        const { error } = await supabase.from(table).select('count').limit(1);
        if (error) {
          missing.push(table);
        }
      } catch (e) {
        missing.push(table);
      }
    }
    
    return {
      exists: missing.length === 0,
      missing
    };
  } catch (error) {
    console.error("Error checking required tables:", error);
    return {
      exists: false,
      missing: requiredTables
    };
  }
};
