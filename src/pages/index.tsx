
// This file is kept for compatibility but we're now using the main routing in App.tsx
// All routes should be defined in App.tsx to avoid conflicts

const AppRouter = () => {
  return null;
};

export default AppRouter;
